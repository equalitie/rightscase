Drupal.behaviors.showMap = function( context ){
  try{
    if( HtmlLoader.jsonArray.latlng!=null ){
      console.log( context );
      
    }
  }catch(err){
  
  }
  //MYMAP.placeMarkers( HtmlLoader.jsonArray.latlng );
  
};	

